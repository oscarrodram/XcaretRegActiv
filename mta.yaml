_schema-version: "3.2"
ID: comxcaretregactivosfijos
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comxcaretregactivosfijos-destination-content
  type: com.sap.application.content
  requires:
  - name: comxcaretregactivosfijos-destination-service
    parameters:
      content-target: true
  - name: comxcaretregactivosfijos-repo-host
    parameters:
      service-key:
        name: comxcaretregactivosfijos-repo-host-key
  - name: comxcaretregactivosfijos-uaa
    parameters:
      service-key:
        name: comxcaretregactivosfijos-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comxcaretregactivosfijos_html_repo_host
          ServiceInstanceName: comxcaretregactivosfijos-html5-service
          ServiceKeyName: comxcaretregactivosfijos-repo-host-key
          sap.cloud.service: comxcaretregactivosfijos
        - Authentication: OAuth2UserTokenExchange
          Name: comxcaretregactivosfijos_uaa
          ServiceInstanceName: comxcaretregactivosfijos-xsuaa-service
          ServiceKeyName: comxcaretregactivosfijos-uaa-key
          sap.cloud.service: comxcaretregactivosfijos
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comxcaretregactivosfijos-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comxcaretregactivosfijos-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comxcaretregactivosfijos.zip
      name: comxcaretregactivosfijos
      target-path: resources/
- name: comxcaretregactivosfijos
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comxcaretregactivosfijos-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comxcaretregactivosfijos-destination-service
    service-plan: lite
- name: comxcaretregactivosfijos-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comxcaretregactivosfijos-xsuaa-service
    service-plan: application
- name: comxcaretregactivosfijos-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comxcaretregactivosfijos-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
